ReChair OMS v0.7.9 적용 안내
================================

이번 버전은 렌탈 상담을 관리자 CRM에서
상담접수 → 견적발송 → 계약완료 → 설치예약 → 운영중까지 관리하는 기능입니다.

[중요] 반드시 SQL을 먼저 실행한 뒤 파일을 덮어쓰세요.

1. Supabase SQL Editor 실행
--------------------------------
압축파일 안의 아래 파일을 메모장으로 열어 전체 내용을 복사합니다.

supabase/v079_rental_quote_contract_crm.sql

Supabase → SQL Editor → New query에 붙여넣고 Run을 누릅니다.
Success. No rows returned가 나오면 정상입니다.

2. 프로젝트 파일 덮어쓰기
--------------------------------
압축을 푼 폴더 안의 app, components 폴더와 파일을
기존 C:\ReChair에 같은 위치로 덮어씁니다.

C:\ReChair 폴더 전체나 .git 폴더는 삭제하지 마세요.

3. GitHub Desktop 반영
--------------------------------
Summary에는 아래 문구를 입력하세요.

Add rental quote and contract CRM workflow v0.7.9

Commit to main → Push origin 순서로 진행합니다.

4. Vercel 배포 확인
--------------------------------
Vercel 배포가 Ready가 된 후 아래 주소에서 테스트합니다.

https://re-chair.vercel.app/admin/consultations

렌탈 상담을 선택했을 때 파란색 '렌탈 견적·계약 관리' 영역이 보여야 합니다.

주의
--------------------------------
- 이 패치는 v0.7.8이 적용된 최신 ReChair 프로젝트 기준입니다.
- 기존 상담, AS 일정, 기사 화면, 상품 관리 기능은 삭제하지 않습니다.
- SQL을 실행하지 않고 코드부터 배포하면 rental_stage 컬럼 오류가 발생합니다.
