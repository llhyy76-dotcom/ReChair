# ReChair OMS v0.7.9 테스트 체크리스트

## 1. SQL 확인

- Supabase SQL Editor에서 `v079_rental_quote_contract_crm.sql` 실행이 성공했는지 확인
- 오류 없이 `Success. No rows returned`가 표시되는지 확인

## 2. 신규 렌탈 상담 접수

- 고객 화면 `/rental`에서 렌탈 상품을 선택
- 상담 신청을 완료
- 렌탈 상담에서는 제품 사진 업로드 항목이 표시되지 않는지 확인
- 관리자 `/admin/consultations`에 해당 상담이 나타나는지 확인
- 선택한 상품의 월 렌탈료·보증금·설치비·계약기간이 자동 입력됐는지 확인

## 3. 렌탈 단계 저장

- 렌탈 상담을 선택했을 때 `렌탈 견적·계약 관리` 패널이 표시되는지 확인
- `조건 확인` 선택 → `렌탈 정보 저장` → 새로고침 후 유지되는지 확인
- `견적 발송` 선택 → 저장 후 목록 상태가 `견적발송`으로 바뀌는지 확인
- `계약 완료` 선택 → 저장 후 목록 상태가 `계약완료`로 바뀌는지 확인
- `설치 예약`, `운영 중`, `계약 종료`도 같은 방식으로 저장되는지 확인

## 4. 계약 정보

- 월 렌탈료, 보증금, 설치비, 계약기간을 수정하고 저장
- 계약번호와 매월 결제일을 입력하고 저장
- 계약 시작일 입력 시 종료일이 자동 제안되는지 확인
- 필요하면 종료일을 직접 수정할 수 있는지 확인
- 설치 예정일과 렌탈 조건 메모가 저장되는지 확인

## 5. 일반 상담 회귀 테스트

- 중고판매·수리 등 일반 상담에는 렌탈 관리 패널이 나타나지 않는지 확인
- 일반 상담의 상태·담당자·견적·메모 저장이 기존처럼 작동하는지 확인
- AS 일정 생성과 AI 배정 링크가 기존처럼 작동하는지 확인

## 6. 모바일 관리자 화면

- 휴대전화에서 렌탈 단계 버튼을 좌우로 움직일 수 있는지 확인
- 금액·계약 입력칸이 화면 밖으로 잘리지 않는지 확인
- `렌탈 정보 저장` 버튼이 화면 폭 안에 표시되는지 확인

## 7. 개인정보 접근 확인

- 관리자 로그아웃 상태에서 `/admin/consultations` 접속 시 로그인 화면으로 이동하는지 확인
- 관리자 로그아웃 상태에서 관리자 상담 API가 고객정보를 반환하지 않는지 확인

