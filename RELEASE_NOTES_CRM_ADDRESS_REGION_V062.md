# ReChair CRM Address/Region UX v0.6.2

## 목적
AI 자동배정 테스트를 위해 상담 CRM에서 주소와 지역을 쉽게 입력/수정할 수 있도록 개선합니다.

## 변경사항
- 상담 상세에 `방문 위치` 카드 추가
- `전체 주소` 직접 입력 가능
- 주소 입력 시 `지역` 자동 인식
- `지역 자동인식` 버튼 제공
- 상담 저장 API가 `region`, `address`를 실제 DB에 저장
- AS 일정 생성 팝업에 `지역` 입력 필드 추가
- 일정 생성 시 주소/지역을 함께 전달

## 테스트
1. 상담 CRM에서 상담 선택
2. 전체 주소에 `경기도 고양시 덕양구 화정동 123` 입력
3. 지역이 `고양시 덕양구`로 자동 입력되는지 확인
4. `변경사항 저장` 클릭
5. AI 배정센터에서 해당 상담 선택
6. `지역 미입력` 경고가 사라지는지 확인
7. AI 추천 실행
