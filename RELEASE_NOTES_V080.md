# ReChair OMS v0.8.0

## PC 렌탈 상품카드 수정

- 상품 이미지와 상품정보를 서로 다른 레이어로 완전히 분리
- 브랜드·모델명·상품명 표시 공간을 고정 확보
- 이미지가 상품명 또는 가격 영역으로 넘어오지 않도록 차단
- 긴 상품명은 최대 두 줄로 정돈
- 구형 상품 데이터 필드와 현재 데이터 필드를 API에서 자동 호환
  - `name` ↔ `title`
  - `model` ↔ `model_name`
  - `image_url` ↔ `thumbnail_url`

## 렌탈관리 접근 개선

- 관리자 상단에 `렌탈관리` 메뉴 추가
- 신규 관리자 주소 `/admin/rental` 추가
- 렌탈 상담만 자동으로 필터링
- 렌탈 상담 선택 시 기존 v0.7.9의 9단계 견적·계약 패널 표시

## 검증

- Next.js 프로덕션 빌드 성공
- TypeScript 타입 검사 성공
- 정적·동적 라우트 42개 생성 확인
- `/admin/rental` 신규 페이지 생성 확인

## SQL

- v0.7.9 SQL을 이미 실행한 사이트에는 추가 SQL 작업 없음
- v0.7.9 SQL 미실행 사이트를 위해 기존 SQL 파일을 패키지에 포함
