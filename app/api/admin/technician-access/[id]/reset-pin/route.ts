import {NextRequest,NextResponse} from 'next/server';import {getSupabaseServer} from '@/lib/supabaseServer';export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,b=await req.json(),pin=String(b.pin||'');if(!/^[0-9]{4,8}$/.test(pin))return NextResponse.json({error:'PIN은 숫자 4~8자리입니다.'},{status:400});const sb=getSupabaseServer();const {error}=await sb.rpc('admin_reset_technician_pin',{p_technician_id:id,p_pin:pin});if(error)throw error;const x=await sb.from('technician_sessions').delete().eq('technician_id',id);if(x.error)throw x.error;return NextResponse.json({success:true})}catch(e:any){return NextResponse.json({error:e?.message||'PIN 변경 오류'},{status:500})}}