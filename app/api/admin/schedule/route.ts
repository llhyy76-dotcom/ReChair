import {NextRequest,NextResponse} from 'next/server';import {getSupabaseServer} from '@/lib/supabaseServer';export async function GET(req:NextRequest){try{const u=new URL(req.url),date=u.searchParams.get('date'),view=u.searchParams.get('view')||'day';let q=getSupabaseServer().from('service_schedules').select('*').order('scheduled_at',{ascending:true});if(date){const s=new Date(date+'T00:00:00'),e=new Date(s);if(view==='week')e.setDate(e.getDate()+7);else e.setHours(23,59,59,999);q=q.gte('scheduled_at',s.toISOString()).lt('scheduled_at',e.toISOString())}const {data,error}=await q;if(error)throw error;return NextResponse.json({data:data||[]})}catch(e:any){return NextResponse.json({error:e?.message||'일정 조회 오류'},{status:500})}}