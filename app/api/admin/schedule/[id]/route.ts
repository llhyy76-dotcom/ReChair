import {NextRequest,NextResponse} from 'next/server';import {getSupabaseServer} from '@/lib/supabaseServer';const S=['배정대기','배정완료','이동중','방문중','완료','취소'];export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params,b=await req.json();if(!S.includes(b.status))return NextResponse.json({error:'허용되지 않은 상태입니다.'},{status:400});const p={scheduled_at:b.scheduled_at,assignee:b.assignee||null,duration_minutes:Number(b.duration_minutes||60),status:b.status,address:b.address||null,memo:b.memo||null,updated_at:new Date().toISOString()};const sb=getSupabaseServer();const {data,error}=await sb.from('service_schedules').update(p).eq('id',id).select('*').single();if(error)throw error;if(data?.consultation_id){const c:any={assignee:p.assignee,next_action_at:p.scheduled_at,updated_at:new Date().toISOString()};if(p.status==='완료')c.status='방문완료';await sb.from('consultations').update(c).eq('id',data.consultation_id)}return NextResponse.json({data})}catch(e:any){return NextResponse.json({error:e?.message||'일정 저장 오류'},{status:500})}}