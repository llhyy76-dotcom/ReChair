'use client';
import { useState } from 'react';
export default function ConsultationForm(){const [done,setDone]=useState(false);async function onSubmit(e:any){e.preventDefault();const form=new FormData(e.currentTarget);const data=Object.fromEntries(form.entries());try{await fetch('/api/consultations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});setDone(true);e.currentTarget.reset();}catch{alert('접수 저장은 DB 연결 후 활성화됩니다.');}}return <section id="consult" className="section"><div className="wrap" style={{maxWidth:880}}><div className="title"><b>Free Quote</b><h2>무료 상담 신청</h2><p>사진 없이도 먼저 접수 가능합니다. 모델명과 지역만 남겨주세요.</p></div><form className="formBox" onSubmit={onSubmit}><div className="grid2"><input name="name" placeholder="이름" required/><input name="phone" placeholder="연락처" required/></div><div className="grid2" style={{marginTop:14}}><select name="service"><option>중고 안마의자 구매</option><option>내 안마의자 판매</option><option>이전설치</option><option>폐기수거</option><option>출장수리</option><option>부품구매</option></select><input name="region" placeholder="지역 예: 경기 고양시"/></div><input style={{marginTop:14}} name="model" placeholder="브랜드/모델명 예: 코지마 CMC-A100"/><textarea style={{marginTop:14}} name="message" placeholder="문의 내용을 적어주세요"/><button>무료 상담 신청</button>{done&&<p style={{color:'#2563eb',fontWeight:900}}>접수되었습니다. 담당자가 연락드립니다.</p>}</form></div></section>}
