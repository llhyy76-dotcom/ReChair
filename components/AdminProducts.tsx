'use client';
import { useEffect, useState } from 'react';
export default function AdminProducts(){const [items,setItems]=useState<any[]>([]);const [form,setForm]=useState<any>({title:'',brand:'',model:'',price:'',status:'판매중',grade:'A',description:''});async function load(){const r=await fetch('/api/products',{cache:'no-store'});setItems((await r.json()).items||[])}useEffect(()=>{load()},[]);async function submit(e:any){e.preventDefault();await fetch('/api/products',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,price:Number(form.price||0)})});setForm({title:'',brand:'',model:'',price:'',status:'판매중',grade:'A',description:''});load()}async function del(id:string){if(confirm('삭제할까요?')){await fetch('/api/products/'+id,{method:'DELETE'});load()}}return <div className="admin-grid"><form className="detail" onSubmit={submit}><h2>상품 등록</h2><input className="input" placeholder="상품명" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} required/><div className="grid2" style={{marginTop:12}}><input className="input" placeholder="브랜드" value={form.brand} onChange={e=>setForm({...form,brand:e.target.value})}/><input className="input" placeholder="모델명" value={form.model} onChange={e=>setForm({...form,model:e.target.value})}/></div><div className="grid2" style={{marginTop:12}}><input className="input" placeholder="판매가" type="number" value={form.price} onChange={e=>setForm({...form,price:e.target.value})}/><select className="select" value={form.grade} onChange={e=>setForm({...form,grade:e.target.value})}><option>A</option><option>B</option><option>C</option></select></div><textarea className="textarea" placeholder="상품 설명" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} style={{marginTop:12}}/><button className="btn" style={{width:'100%',marginTop:12}}>상품 등록</button></form><div className="list"><h2>상품 목록</h2>{items.map(p=><div className="list-item" key={p.id}><b>{p.title}</b><p>{p.brand} {p.model}</p><p>{Number(p.price||0).toLocaleString()}원 · {p.status}</p><button className="btn btn-dark" onClick={()=>del(p.id)}>삭제</button></div>)}</div></div>}
