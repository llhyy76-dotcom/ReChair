import Link from 'next/link';import { products } from '@/lib/data';
export default function ProductList(){return <section className="section" style={{background:'white'}}><div className="wrap"><div className="title"><b>Used Products</b><h2>판매 중인 중고상품</h2></div><div className="products">{products.map(p=><Link className="product" href={`/products/${p.id}`} key={p.id}><div className="thumb">🪑</div><div className="productBody"><span className="tag">{p.grade}급 · {p.area}</span><h3>{p.brand} {p.model}</h3><p>{p.desc}</p><div className="price">{p.price}</div></div></Link>)}</div></div></section>}
