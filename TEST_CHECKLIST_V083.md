# ReChair OMS v0.8.3 테스트 체크리스트

## 1. 적용 확인

- [ ] `supabase/v083_rental_retrieval_inventory.sql` 실행 결과가 `Success. No rows returned`이다.
- [ ] GitHub Desktop에서 v0.8.3 파일을 Commit하고 Push했다.
- [ ] Vercel 배포 상태가 `Ready`이다.
- [ ] `/admin/rental/operations`가 오류 없이 열린다.

## 2. 테스트 준비

- [ ] 렌탈 계약 한 건이 `운영중` 또는 `계약종료` 단계이다.
- [ ] 해당 계약에 렌탈 상품이 연결되어 있다.
- [ ] 자동 상품 상태 전환 테스트용 상품의 `stock_qty`가 1이다.
- [ ] 활성화된 기사 계정이 한 명 이상 있다.

## 3. 회수 일정 생성

1. `/admin/rental/operations`에서 테스트 계약을 선택합니다.
2. 회수일시, 담당기사, 소요시간, 회수사유를 입력합니다.
3. `렌탈 회수 일정 저장`을 누릅니다.

- [ ] 저장 완료 메시지가 보인다.
- [ ] 계약 상태가 `계약종료`로 유지된다.
- [ ] 운영센터 목록에 `회수 진행` 상태가 표시된다.
- [ ] 현장 캘린더에 `렌탈 회수` 일정이 표시된다.
- [ ] 재고 1대 상품은 `회수예정` 및 비공개로 전환된다.

## 4. 기사 모바일 작업

1. 배정된 기사로 로그인합니다.
2. 기사 화면에서 `렌탈 회수` 업무를 엽니다.
3. 출발 → 도착 → 작업시작 순서로 진행합니다.
4. 작업보고에 아래 내용을 입력합니다.

- 반납상태: `정상`
- 후속처리: `재렌탈가능`
- 앞면·측면·라벨 사진
- 회수 내용과 고객 확인
- 고객 서명

- [ ] 필수 항목이 없으면 최종 제출이 차단된다.
- [ ] 모든 항목 입력 후 `회수보고 저장 및 제출`이 성공한다.
- [ ] 관리자 화면에서 상태가 `검토대기`로 보인다.

## 5. 관리자 승인 및 재고 전환

1. `/admin/schedule`에서 해당 렌탈 회수 일정을 엽니다.
2. `작업보고 보기`에서 사진·서명·반납상태·후속처리를 확인합니다.
3. 승인합니다.

- [ ] 운영센터에서 `회수완료`로 표시된다.
- [ ] 계약의 회수완료 시각과 반납상태가 저장된다.
- [ ] 상품이 `렌탈가능`으로 바뀌고 고객 렌탈 페이지에 다시 공개된다.

## 6. 예외 테스트

- [ ] 후속처리 `점검필요` 승인 시 상품이 `점검중`·비공개가 된다.
- [ ] 후속처리 `정비필요` 승인 시 상품이 `정비중`·비공개가 된다.
- [ ] 후속처리 `폐기검토` 승인 시 상품이 `폐기검토`·비공개가 된다.
- [ ] 보고서를 반려하면 기사 화면에 반려 사유가 표시된다.
- [ ] 회수 일정을 취소하면 계약은 `계약종료`로 유지되고 운영센터에 `회수 일정 필요`로 표시된다.
- [ ] `stock_qty`가 2 이상인 연결 상품은 전체 상품 상태가 자동 변경되지 않는다.

## 오류가 날 때 확인할 것

- SQL을 먼저 실행했는지 확인합니다.
- 선택한 기사가 활성 상태인지 확인합니다.
- 주소와 지역이 계약에 저장되어 있는지 확인합니다.
- 동일 시간대에 기사 일정이 겹치지 않는지 확인합니다.
- 화면의 실제 오류 문구와 Vercel Build/Runtime Logs를 함께 확인합니다.
