# ReChair OMS v0.8.1 테스트 체크리스트

## 1. SQL 및 배포

- [ ] Supabase SQL Editor에서 `supabase/v081_rental_installation_operations.sql` 실행
- [ ] SQL 실행 결과가 Success인지 확인
- [ ] 패치 파일을 `C:\ReChair`에 경로 그대로 덮어쓰기
- [ ] GitHub Desktop에서 Commit 후 Push origin
- [ ] Vercel 배포 상태가 Ready인지 확인

## 2. 렌탈 설치 일정 생성

1. `/admin/rental`에 접속합니다.
2. 테스트할 렌탈 상담을 선택합니다.
3. 방문 위치의 지역과 전체 주소를 저장합니다.
4. 렌탈 단계를 `계약완료`로 선택하고 `렌탈 정보 저장`을 누릅니다.
5. 설치 예정일을 입력합니다.
6. `설치 일정·기사 연동`에서 담당기사와 예상 설치시간을 선택합니다.
7. `설치 일정 생성`을 누릅니다.

정상 결과:

- [ ] 현재 렌탈 단계가 `설치예약`으로 변경됨
- [ ] 설치 일정 상태가 `배정완료`로 표시됨
- [ ] "기사 화면과 현장 캘린더에 반영" 안내가 표시됨
- [ ] `현장 캘린더에서 보기` 버튼이 나타남

## 3. 현장 캘린더 확인

1. `현장 캘린더에서 보기`를 누릅니다.
2. 지정한 날짜에 고객 일정이 표시되는지 확인합니다.

정상 결과:

- [ ] 일정에 파란색 `렌탈 설치` 표시가 보임
- [ ] 고객명, 주소, 담당기사, 설치시간이 맞음
- [ ] 일정 수정 후 렌탈 상담의 설치 예정일도 함께 변경됨

## 4. 기사 화면 확인

1. 배정한 기사 계정으로 `/technician/login`에서 로그인합니다.
2. 설치 예정일을 선택합니다.

정상 결과:

- [ ] 방문 일정에 `렌탈 설치` 표시가 보임
- [ ] 고객명, 연락처, 주소, 렌탈 상품/계약 메모가 보임
- [ ] `출발하기 → 도착 처리 → 작업 시작 → 작업보고 작성` 순서로 진행됨
- [ ] 작업보고와 고객서명을 최종 제출하면 `승인 대기`로 표시됨

## 5. 관리자 승인 및 운영중 전환

1. 관리자 `현장 캘린더`에서 해당 설치 일정을 엽니다.
2. `작업보고 보기`를 누릅니다.
3. 보고 내용을 확인하고 `승인`합니다.
4. `/admin/rental`로 돌아가 해당 고객을 다시 선택합니다.

정상 결과:

- [ ] 작업보고 상태가 `승인완료`로 표시됨
- [ ] 렌탈 9단계의 현재 단계가 자동으로 `운영중`이 됨
- [ ] 일반 상담 처리상태도 `운영중`으로 동기화됨

## 6. 반려 확인

별도 테스트 건으로 작업보고를 반려합니다.

- [ ] 반려 사유 입력 없이 반려할 수 없음
- [ ] 반려 후 렌탈 단계는 `설치예약`으로 유지됨
- [ ] 기사 화면에 반려 사유가 표시됨
- [ ] 보완한 작업보고를 다시 제출할 수 있음

