# ReChair OMS v0.6.1 — AI 배정 확정 연동

## 목적
AI 추천 결과를 단순 비교 화면에서 끝내지 않고 실제 AS 일정으로 확정하는 흐름을 완성합니다.

## 변경 사항
- AI 추천 API가 TOP3와 신뢰도를 공식 반환합니다.
- 기존 자동배정 화면 호환 필드는 그대로 유지합니다.
- AI 추천 TOP3 중 관리자가 직접 기사를 선택할 수 있습니다.
- `선택 기사로 배정 확정` 버튼이 추가됩니다.
- 배정 확정 시 기존 일정 생성 API를 사용하므로 다음 검증을 다시 수행합니다.
  - 기사 활성 여부
  - 근무/휴무 여부
  - 시간 충돌
  - 일일 처리한도
  - 동일 상담 중복 일정
- 확정 성공 시 `service_schedules`에 일정이 생성됩니다.
- 상담의 assignee / next_action_at이 같이 갱신됩니다.
- AS 캘린더와 기사 모바일 일정에서 같은 데이터를 보게 됩니다.

## DB 변경
없음.

## Commit Summary
`Connect AI dispatch to schedule assignment v0.6.1`

## 롤백
이 패치의 3개 코드 파일을 직전 v0.6 버전으로 되돌리면 됩니다.
